---
apiVersion: v1
kind: Service
metadata:
  name: app-c
  namespace: ns-c
  labels:
    app: app-c
    service: app-c
spec:
  ports:
    - port: 80
      targetPort: 8000
      name: http
    - port: 3000
      targetPort: 3000
      name: tcp
  selector:
    app: app-c
